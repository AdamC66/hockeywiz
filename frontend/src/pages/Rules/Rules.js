import React from 'react'

import Document from '../../components/Document/Document'

function Rules() {

    return (
        <div style={{margin: '1em auto', width: '600px', textAlign: 'center'}}>
            <Document></Document>
        </div>
    )
}

export default Rules
